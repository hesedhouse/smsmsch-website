# 생명샘명성교회 설교 백필 — 세션로그

저장소: `hesedhouse/smsmsch-website` (GitHub, **public**)
작업 폴더: `Z:\hesedcorp\98_hesedAI\_작업_교회웹사이트`

---

## 현재 상태 (2026-08-01, 회사 PC)

채널 총 **121편** (전부 주일예배 설교, 2021~2026). 전부 제목에 날짜·성경본문이 규격대로 있음.

| 구분 | 수 |
|---|---|
| 게시 완료 (유튜브 자막 기반) | 25 |
| 백필 대상 (자막 없음 → 로컬 STT) | **96** |

**진행 중: 96편 로컬 Whisper 전사 배치** (1편 5~6분, 총 8~10시간)
로그: `transcribe_batch.log` / 결과: `transcripts/{videoId}.txt`

---

## 왜 96편이 막혀 있었나

기존 `publish_sermon.py`는 유튜브 자동자막에만 의존했다. 자막 없는 영상은
1단계에서 예외가 나고 스킵된다. 자막 있는 25편은 이미 전부 소진된 상태였다.

---

## 구성

- **GPU**: GTX 1070 Ti (Pascal, CC 6.1)
  - `float16` 미지원 → `int8_float32` 사용 (CT2 지원 목록: `int8`, `int8_float32`, `float32`)
  - cuBLAS/cuDNN DLL은 **PATH에 직접 넣어야** 한다. `os.add_dll_directory`로는 ctranslate2가 못 찾음
- **ffmpeg 불필요**: yt-dlp로 원본 오디오만 받고 faster-whisper가 PyAV로 직접 디코딩
- 모델: `large-v3`, VAD 필터 on, `condition_on_previous_text=False` (반복 환각 억제)

## A/B 검증 결과 (ZlMfoOr4Weg, 이미 게시된 편으로 대조)

오류 성격이 다른 **무승부**. 유튜브가 구두점은 더 많고(379 vs 121) `여호와`를 잘 잡음.
Whisper는 문장이 완결되고 잡음 태그가 없으며 `에벤에셀`을 더 잘 잡음(7 vs 4).
**핵심: 96편엔 자막이 아예 없으므로 Whisper로 충분한지가 유일한 질문이고, 충분하다.**

고유명사 오인식은 2중으로 보완:
1. `INITIAL_PROMPT`에 성경 인명·지명·용어 90여 개 (법궤·블레셋·시내산 포함)
2. `TERM_FIXES` 확정 치환 (법괴→법궤, 블랙셋→블레셋, 신해산→시내산 등)
3. 요약 프롬프트에도 문맥 교정 지시 + "불확실하면 지어내지 말 것" 규칙

---

## 남은 절차 (전사 완료 후) — ⚠️ 회사 PC에서 실행

**전사(집 PC GPU) → 요약(회사 PC)** 으로 나뉜다.

`ANTHROPIC_API_KEY`가 **집 PC에는 없다** (User/Machine/Process 스코프 전부 확인).
기존 25편을 회사 PC에서 처리했으므로 키는 회사 PC에 있을 것으로 본다.

작업 폴더와 `transcripts/`가 **Z 드라이브(양쪽 PC 공유)** 에 있으므로,
집 PC에서 전사만 끝내두면 회사 PC는 아래 한 줄이면 된다.
전사본이 캐시에 있으면 GPU 없이 요약만 수행한다.

```bash
cd "Z:\hesedcorp\98_hesedAI\_작업_교회웹사이트"
python -u scripts/publish_sermon.py --backfill --stt
```

그 다음 결과 확인하고 푸시:

```bash
git add -A
git commit -m "feat: 설교 말씀 96편 백필 게시 (로컬 STT 전사본 기반)"
git push origin master
```

> ⚠️ 이 저장소의 기본 브랜치는 `main`이 아니라 **`master`** 다.

`transcripts/`·`audio_cache/`는 `.gitignore` 대상이라 **전사 원문은 저장소에 올라가지 않는다.**
(저장소가 public이므로 이게 중요하다.)

### 회사 PC에도 키가 없다면

셋 중 하나로 간다.
1. `Z:\hesedcorp\.env` 에 `ANTHROPIC_API_KEY=...` 한 줄 추가 (양쪽 PC 공유되므로 1회로 끝)
2. 클로드가 전사본을 직접 읽고 요약 JSON을 만들어 렌더링 — 키 불필요, 대신 여러 턴 소요
3. `.github/workflows/backfill-summarize.yml` (작성해둠) 로 Actions에서 실행
   — 단 전사 원문을 임시 브랜치에 올려야 하고, 시크릿 존재 여부가 미검증이다

---

---

## 앞으로의 자동화 방식 — **로컬 PC로 확정** (2026-08-01)

새 영상 주간 자동 게시는 **로컬 PC에서** 처리한다. GitHub Actions·NAS 모두 검토 후 배제.

### 근거: 유튜브가 클라우드 IP에서 자막 API를 차단한다

Actions에서 `publish-sermon.yml`을 수동 실행해 실측한 결과다.

```
yt-dlp 영상목록 조회      → 성공 ("처리할 영상: 4개")
youtube-transcript-api    → 4편 전부 실패
  "YouTube is blocking requests from your IP.
   You are doing requests from an IP belonging to a cloud provider"
```

즉 Actions에서는 **자막을 하나도 못 가져온다.** 그러면 100% STT로 넘어가는데
Actions에는 GPU가 없어 CPU로 1~3시간이 걸린다. (오디오 다운로드 가능 여부도 미검증)

가정용·사무실 IP에서는 자막이 정상 동작한다. 최근 영상 기준 **86%가 자막 보유**
(2025-11-23부터 자막 생성 시작, 그 이전 78편은 0%). 즉 로컬에서는 대부분 STT 없이 끝난다.

### 배제 사유
- **GitHub Actions**: 위 IP 차단. 프록시는 유료, 쿠키 인증은 라이브러리가 계정 영구정지 위험을 경고
- **시놀로지 NAS**: 자막 문제는 해결되나(사무실 IP·24시간) 저전력 CPU라 Whisper 불가.
  자막 없는 14%를 처리 못 함

### 회사 PC에서 할 일
1. 96편 요약·게시 (위 "남은 절차" 참조)
2. **주간 자동화 등록** — Windows 작업 스케줄러, 매주 월요일
   `python scripts/publish_sermon.py --stt`
   - "예약 시각을 놓친 경우 가능한 한 빨리 실행" 옵션 필수 → PC가 꺼져 있어도 다음 부팅 시 실행
   - GPU 있는 PC여야 자막 없는 14%까지 처리된다. 없으면 그 편은 계속 대기
3. `publish-sermon.yml`의 주간 cron은 **비활성화**하는 게 낫다 (자막 차단으로 어차피 실패)

---

## 주의사항

- **주간 자동 게시(`publish-sermon.yml`)는 건드리지 않았다.** 다만 위에 적었듯
  Actions에서는 자막 API가 IP 차단되므로 이 워크플로는 사실상 동작하지 않는다.
  로컬 스케줄러로 대체 후 cron을 끄는 게 낫다.
- `.github/workflows/backfill-summarize.yml`은 작성만 해두고 **쓰지 않기로 했다.**
  (로컬 요약으로 방향 확정. 삭제해도 무방)
- 배치가 중단돼도 `transcripts/` 캐시가 남아 재실행 시 이어서 처리된다.
  전사(GPU 10시간)와 요약(API)을 분리해둔 이유가 이것이다.
- `blog/2026-07-26-에벤에셀의하나님.html` 은 초기 테스트 잔재 고아 파일.
  `sermons.json`에 없어서 목록에 안 뜬다. **삭제 여부 대표 확인 대기 중.**
- 채널 121편 중 1편(`생명샘명성교회 2021년 11월 3일 수요기도회`)만 제목에 따옴표
  설교제목이 없어 slug가 길게 잡힌다. 게시 후 확인 필요.
- 저장소는 **public**이며 `has_pages: false`. 현재 사이트가 어디에 배포 중인지는 미확인.

---

## 세션 로그

- **2026-08-01 (회사 PC)** — 백필 파이프라인 구축. GPU 환경 검증(int8_float32 확정,
  cuBLAS PATH 이슈 해결), A/B 검증 1편 실행, `transcribe_local.py` 신규 작성,
  `publish_sermon.py`에 STT 폴백·`--backfill`·`--transcribe-only` 추가,
  `backfill-summarize.yml` 작성. 고유명사 오인식 확인 후 프롬프트·치환 보강하여
  96편 전사 배치 재시작.
